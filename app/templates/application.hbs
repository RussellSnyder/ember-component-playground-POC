<div class="container">


<div class="row justify-content-center">
  <h1>PLAYGROUND PROTOTYPE</h1>
  <div class="col-12 text-center">
    <Playground @variations={{variations}} as | selected |>
      <MayaButton
        @disabled={{selected.disabled}}
        @type={{selected.type}}
        @size={{selected.size}}
        @active={{selected.active}}
      >
        Test
      </MayaButton>
    </Playground>
  </div>
</div>
</div>

{{outlet}}